[build-system]
requires = ["flit_core >=2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "grepxl"
version = "1.0.0"
description = 'Search fields in excel'

[project.scripts]
grepxl = "grepxl:main"
