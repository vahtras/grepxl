[build-system]
requires = ["flit_core >=2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "grepxl"
version = "1.0.0"
description = 'Search fields in excel'
dependencies = [
    "openpyxl==3.0.10",
    "pytest==7.2.0",
    "rich-tools==0.5.0",
]

[project.scripts]
grepxl = "grepxl:main"
